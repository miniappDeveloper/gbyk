<!--pages/auth/auth.wxml-->
<view wx:if="{{!hasUserInfo}}" class='userinfo_auth'>
  <image src='/images/bg.png'></image>
  <button wx:if="{{canIUse}}" open-type="getUserInfo" plain='true' bindgetuserinfo="bindGetUserInfo">登录</button>
</view>